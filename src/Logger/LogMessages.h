#include <sys/types.h>
#include <unistd.h>

#define CREACION_BARCO_EXITO(id) "Se ha creado el barco con ID: " + std::to_string(id) + " con exito. \n"
#define CREACION_BARCO_ERROR(id) "Error al intentar crear el barco con ID: " + std::to_string(id) + ". \n"
#define BARCO_SALE_A_NAVEGAR(id) "El barco con ID: " + std::to_string(id) + " sale a navegar. \n"
#define CREACION_BARCO_ERROR(id) "Error al intentar crear el barco con ID: " + std::to_string(id) + ". \n"

#define CREACION_PUERTO_EXITO(id) "Se ha creado el PUERTO con ID: " + std::to_string(id) + " con exito. \n"
#define CREACION_PUERTO_ERROR(id) "Error al intentar crear el PUERTO con ID: " + std::to_string(id) + ". \n"


